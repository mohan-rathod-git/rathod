# Banjara Bandhan v4.0 — Progress Log

| Timestamp | Phase | Summary |
|---|---|---|
| 2026-07-23 18:58 | Audit | Completed. Codebase audited against v4.0 spec. 12-phase plan approved by user. |
| 2026-07-23 19:06 | Phase 1 | **Security & Privacy Foundation** — Completed. Created `canView()` privacy matrix, `useMatchStatus` hook, `is_matched()` Postgres function, engagement event logger. Updated ProfileDetail with privacy filtering, match-gated messaging, and state-machine bottom actions. Migration SQL ready for deployment. Build passes. |
| 2026-07-23 19:15 | Phase 2 | **Responsive Rebuild & Motion** — Overhauled `index.css` with fluid typography (`clamp()`), safe area padding, 44px min touch targets, container query utilities, and global `prefers-reduced-motion` compliance. |
| 2026-07-23 19:20 | Phase 3 | **Multi-Language (i18n)** — Created 4 translation bundles (`en.json`, `hi.json`, `kn.json`, `te.json`), `i18n/index.ts` setup, `LanguagePicker` component, and integrated into Login hero and Settings page. |
| 2026-07-23 19:22 | Phase 4 | **Recommendation Algorithm** — Built weighted scoring engine (`recommendationEngine.ts`) with hard filters, preference overlap, freshness, reciprocity, and diversity injection. Integrated into `Index.tsx` feed. |
| 2026-07-23 19:24 | Phase 5 & 6 | **Photo Privacy & Gotra Exogamy** — Photo blur/filtering via privacy matrix. Built Gotra validator (`gotraValidator.ts`) & `GotraWarningBanner.tsx` for same-gotra match advisory. Integrated in `ProfileDetail.tsx`. |
| 2026-07-23 19:25 | Phase 7 & 8 | **Notifications & Reduced Motion** — Created real-time `useNotifications` hook and animated `NotificationBell` with unread count dropdown panel. Integrated in `HomeHeader.tsx`. Global motion reduction active. |
| 2026-07-23 19:26 | Phase 9 | **Block / Report System** — Built `blockReport.ts` utility for instant user block/unblock and structured reporting. Integrated with `ProfileDetail.tsx` actions. |
| 2026-07-23 19:27 | Phase 10 | **Content Moderation** — Built `moderation.ts` for filtering profanity, phone numbers, email addresses, and links in messages. Integrated into `Chat.tsx` handleSend flow. |
| 2026-07-23 19:28 | Phase 11 & 12 | **Performance, Security & Verification** — Added enterprise security headers to `vercel.json` (HSTS, Referrer-Policy, Permissions-Policy), verified Helmet SEO setup, ran `npx tsc --noEmit` (0 errors), and confirmed production build (`npm run build`). |
| 2026-07-23 22:45 | Admin Dashboard | **Realtime Secure Admin Suite** — Built server-side RLS policies (`20260723_admin_dashboard.sql`), immutable `admin_audit_log`, `useAdminRole` hook (30-min idle timeout), `AdminRoute` guard, `AdminLayout` sidebar, Dashboard overview cards, User management, Abuse reports queue with auto-escalation, Broadcast notifications with rate limiting, CSS Analytics charts & conversion funnel, and Audit log viewer. Verified clean build (`npm run build`). |
| 2026-07-23 23:59 | Settings & Features | **Settings Fixes, Quota, Crop, Friend Tier & Hero** — (1) Fixed Hide Profile via `is_hidden` column + RLS + ProfileDetail guard. (2) Consolidated LanguagePicker to Settings only. (3) Consolidated Privacy/Terms/Guidelines into `/legal` with sticky TOC & redirects. (4) 50MB storage quota enforced via `increment_storage` DB RPC & `storageQuota.ts`. (5) Integrated 1:1 `CropModal` (react-easy-crop). (6) Friend tier layered on MATCHED for voice/video call gating. (7) Created vector-only `PhoneMockup` & `LandingHero` with admin editor (`AdminLandingContent.tsx`). All 0 errors on `tsc` & `npm run build`. |
| 2026-07-24 12:42 | Feature Audit | **Broken Features Found:** (1) 🔴 LanguageSettings wrote to `i18nextLng` but i18n config reads `bb_language` — language did not persist across refresh. Fixed. (2) 🟢 Hide Profile toggle — correctly wired with optimistic UI + rollback. (3) 🟢 Notification Preferences — upsert to DB with rollback. (4) 🟢 Dark Mode — localStorage persist. (5) 🟢 Blocked Users — full CRUD. (6) 🟢 Delete Account — type-DELETE confirm flow. (7) 🟢 Storage Quota display — reads from profile. |
| 2026-07-24 12:55 | Romantic Chat | **Couple Chat Experience Upgrade** — (1) Created `chatThemes.ts` with 6 curated themes (Saffron Love, Rose Garden, Golden Hour, Midnight Plum, Ocean Breeze, Forest Whisper) and per-match localStorage persistence. (2) Built `ChatThemePicker.tsx` — bottom sheet with visual swatches. (3) Built `ChatComposerBar.tsx` — auto-resize textarea, emoji toggle, attachment, mic→send morph with glassmorphism per theme. (4) Built `MessageReactions.tsx` — radial picker (❤️😂😮😢🔥), spring pop animation (scale 0→1.15→1, 350ms), double-tap-to-heart. (5) Built `FirstMessageCelebration.tsx` — one-time heart particle burst per match. (6) Overhauled `Chat.tsx` — directional bubble animations (sent from right, received from left), theme-aware backgrounds/bubbles, delivered/seen fade transitions, empty-state SVG heart decorations. All animations respect `prefers-reduced-motion` via `useReducedMotion()`. |
| 2026-07-24 13:00 | Vercel Fix | **Deployment Error Fix** — Root cause: `vite.config.ts` used `require("lovable-tagger")` (CommonJS) inside an ESM project (`"type": "module"`). Works locally via Vite's special config handling but fails on Vercel's standard Node runtime. Fix: replaced `require()` with dynamic `import()`. Also added `engines: { node: ">=18" }` to `package.json` to prevent Node version mismatches. Added `/auth/v1/callback` to PWA workbox deny list to prevent service worker from intercepting OAuth callbacks. **Do not remove the engines field or revert to require().** |
| 2026-07-24 13:05 | Branding | **Logo Consistency Audit** — Found 3 different logo designs: `logo.jpg` (couple-heart on dark, master brand), `pwa-192.png` (mandala-hearts on cream, 928KB bloated), `pwa-512.png` (same mandala, 15KB low-quality). Unified all `index.html` references to `logo.jpg`. Added apple-touch-icon size variants. Note: PWA icons should be regenerated from `logo.jpg` master to match — current `pwa-*.png` files are visually different designs. |
| 2026-07-24 13:10 | Google OAuth | **OAuth Diagnosis** — Client-side code in `Login.tsx` is correct (`signInWithOAuth` with `redirectTo`). Issue is configuration-side: requires Google Cloud Console (OAuth credentials + redirect URI), Supabase Dashboard (provider toggle + secrets), and Supabase Auth Settings (Site URL + Redirect URLs). Created `OAUTH_SETUP_CHECKLIST.md` with step-by-step instructions. Fixed PWA workbox to not intercept `/auth/v1/callback`. |
| 2026-07-24 13:15 | README | **README.md Rewrite** — Full rewrite from placeholder. Includes project description, feature list (all phases), tech stack table, setup/install instructions, env var names (no secrets), project structure, deployment notes, and brand-aligned tone. |
| 2026-07-24 13:18 | i18n Fix | **Language Settings Refactor** — Refactored `LanguageSettings.tsx` to import `SUPPORTED_LANGUAGES` directly from `@/i18n`. Fixed incorrect native script labels for Marathi (`मराठी`) and Kannada (`ಕನ್ನಡ`). Verified 0 errors on `tsc` and `npm run build`. |


